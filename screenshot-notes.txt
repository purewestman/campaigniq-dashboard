Screenshot Review — Search + Gap Override Update
=================================================

Dashboard renders correctly with zero TypeScript/LSP errors.

New features implemented:
1. Search bar: Controlled input in header, wired to Home.tsx state
   - Filters partners by name, tier label, action text, and email
   - Shows search indicator bar with result count and "Clear search" button
   - All components (KPIs, charts, donut, table) update dynamically
2. Gap Override system: OverrideProvider context with localStorage persistence
   - Each requirement bar in expanded row has "Mark Complete" button
   - Comment input with optional text before confirming
   - Override badge shows who marked it, when, and the comment
   - "Undo" button to remove overrides
   - Override count badge shown next to partner name in table row
   - Overrides persist across page refreshes via localStorage

Visible elements confirmed:
- Sidebar, header, KPI cards, tier summary cards, charts all render correctly
- 23 partners, 31% enablement, 175 gaps, 43 exams
